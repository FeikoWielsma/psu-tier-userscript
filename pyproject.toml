[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
# Bug-finding + cleanliness: pycodestyle, pyflakes, isort, bugbear,
# pyupgrade, flake8-simplify, flake8-blind-except, Ruff-specific.
select = ["E", "F", "W", "I", "B", "UP", "SIM", "BLE", "RUF"]
